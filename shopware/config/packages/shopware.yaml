shopware:
    admin_worker:
        enable_admin_worker: false
    increment:
        # Drop the per-request DB writes; the data is only admin telemetry.
        user_activity:
            type: 'array'
        message_queue:
            type: 'array'
